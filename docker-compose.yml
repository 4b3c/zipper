services:
  discord:
    build: .
    restart: unless-stopped
    env_file: .env
    command: python -u discord_bot.py
    ports:
      - "4200:4200"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/app/data
